namespace SB.Domain
{
    public static class DomainStrings
    {
        public const string
            InvalidData = "Invalid data supplied.",
            IdMustBeOverZero = InvalidData + " ID must be over zero.",
            TermLengthMustBeOverZero = InvalidData + " Term length must be over zero.";
    }
}